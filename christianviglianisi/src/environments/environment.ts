// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  deliveryLeadText: "la mia sfida ogni giorno è trovare il perfetto balance tra il superare le aspettative del cliente e fare in modo che il mio team sia sempre motivato. Credo fermamente che la prima dipenda dalla seconda. " +
      "Garantisco tramite il coordinamento di team di sviluppo l'erogazione dei servizi nei tempi e con la qualità prevista, stabilisco le metodologie e supervisiono il codice. " +
      "Identifico le aree di miglioramento tecnologico e sono l'interfaccia verso i fornitori. Mi occupo di formare i colleghi orientandoli agli obiettivi e all'affidabilità. ",
 softwareEngineerText: "Mi occupo principalmente della progettazione di applicazioni web, intrappolato tutti i giorni tra archetipi, modelli logici e design patterns. "+
  "Ciò che adoro di più del mio lavoro è poter progettare e rivedere l'architettura dietro ogni soluzione, dibattere e discutere insieme ai colleghi con l'unico fine di trovare la via "+
  "più opportuna e stilisticamente più all'avanguardia per risolvere un problema. "
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
